/*

import 'package:cloud_firestore/cloud_firestore.dart';
class Influencer {
  String id;
  String name;
  String image;
  String desc;
  dynamic order;
  bool active;
  Influencer({
    required this.id,
    required this.name,
    required this.image,
    required this.desc,
    this.order,
    required this.active,
  });
  factory Influencer.fromMap(Map data) {
    
    return Influencer(
      id: data['id'],
      name: data['name'],
      image: data['image'],
      desc: data['desc'],
      order: data['order'],
      active: data['active']

    );
  }

}*/
